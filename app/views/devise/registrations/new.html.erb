<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <% if params[:plan] == '2' %>
      <h1>Pro Account</h1>
      <p>Regístrate con una cuenta Pro por $1,200 al mes, ten acceso a nuestra comunidad,información de contacto y acceso a los archivos de los cursos.</p>
    <% else %>
      <h1>Basic Account</h1>
      <p>Regístrate gratis y obtén acceso a nuestra comunidad.</p>
    <% end %>
  </div>
  <div class="col-md-6 col-md-offset-3">
    <div class="well">
      <h2>Regístrate</h2>

        <% if params[:plan] == '2' %>
          <%= render "pro_form" %>
        <% else %>
          <%= render "basic_form" %>
        <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>